import 'package:expensee/components/buttons/custom_callback_button.dart';

// purely for readability
class SignInWithPasswordButton extends CustomCallbackButton {
  const SignInWithPasswordButton(super.child, super.onTap, {super.key});
}
